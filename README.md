# calculator
making a calculator! 

right final project before moving on to the big stuff! 

okay well obviously we will start with the file structure.

html , css , JS. 

then I would like to start with HTML.

we will need buttons for each number and operator. 

numbers = 0123456789
operators = +-/*=

later we will need to look at adding the "." to allow decimal points. 

next we will need a display window to display our selections and sum. 

that should be enough HTML to start off with, we will leave CSS till later. I would like to have a running product before i style it. 

next will be JS. 

we will start simple. a function for each operator. 

plus will add two numbers together

minus will minus the minus two numbers together. 

multiply will multiple 2 numbers together

divide will divide to numbers togther.

then equals will show the sum. 

as for the buttons. 

each number button will show its respective number on the display and store that number as the first parameter for the operator that is selected. 

then the next number that is pressed will be put into the operator function as the second paramter. 

then when you press equals it will return the sum and display that on the screen. 

so acctually not going to end there. 

ADD. 

my functions need to be more dynamic. I know i need to use some kind of array here but im just not that confident in it. lets see how we go and we can have AI help me get where I want to go. 

my functions need to TAKE the NUMBER from DISPLAY and then STORE it, then and wait for the next number. 

okay we have been able to get the numbers to show. now i need to know how i can grab those numbers and store them in my function. 


okay so what I want:

press button ONE TWO, DISPLAY SHOWS 12. 

press button PLUS - function ADDSUM runs taking DISPLAY VALUE as ARGS

ADDSUM STORES DISPLAY VALUE (12)

press button ONE TWO again, DISPLAY SHOWS 12

IF press buton PLUS, DISPLAY SHOWS VALUE OF SUM (24)

Then i press button ONE TWO DISPLAY SHOWS 12. 

if I press PLUS OR EQUALS DISPLAY shows 36. 

